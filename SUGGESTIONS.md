### Suggestions for `bye.py`

1. **[Moderate] [Type Hints]**: Add type hints to all function signatures for better readability and to help with static analysis tools.
2. **[Low] [Docstrings]**: Include docstrings for all functions to describe their purpose and parameters.
3. **[Low] [Separation of Concerns]**: Consider separating logic from I/O (e.g., returning values instead of directly printing) to improve testability.
4. **[Low] [Variable Usage]**: Store intermediate results in variables before printing for better readability and debugging.
5. **[Low] [Function Type Hint]**: Add a return type hint (`-> None`) for the `main` function to explicitly indicate it doesn't return a value.

---

